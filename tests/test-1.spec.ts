// import { test, expect } from "@playwright/test";

// test("test", async ({ page }) => {
//   // Recording...
// });
// await page.goto("https://teststore.automationtesting.co.uk/index.php");
// await page.getByRole("link", { name: "Clothes" }).click();
// await page.getByTitle("Women").click();
// await page.getByRole("link", { name: "Brown bear printed sweater" }).click();
// await page.getByRole("button", { name: " Add to cart" }).click();
// await page.getByRole("link", { name: " Proceed to checkout" }).click();
// await expect(
//   page.getByRole("heading", { name: "Shopping Cart" })
// ).toBeVisible();
// await expect(
//   page.getByRole("link", { name: "Hummingbird printed sweater" })
// ).toBeVisible();
// await expect(page.locator("#main")).toContainText(
//   "Hummingbird printed sweater"
// );
// await expect(
//   page.getByRole("spinbutton", { name: "Hummingbird printed sweater" })
// ).toBeVisible();
// await expect(page.getByRole("link", { name: "delete" })).toBeVisible();
// await expect(page.locator("#cart-subtotal-products")).toContainText("$28.72");
// await expect(
//   page.getByRole("link", { name: "Proceed to checkout" })
// ).toBeVisible();
// await page.getByRole("link", { name: "delete" }).click();
// await expect(page.locator("#main")).toContainText(
//   "There are no more items in your cart"
// );
